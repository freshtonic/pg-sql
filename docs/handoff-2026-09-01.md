# PostgreSQL 17.9 port status — 2026-09-01

This supersedes the stalled-state sections of `handoff-2026-08-31.md`.

## Resolved

The strict-statement blocker is fixed as a general Recursa capability, not a
PostgreSQL exception. Recursa now freezes a suffix-proving optional-viability
decision when an optional element's FIRST overlaps caller FOLLOW and the
element's bounded prefix language is closed: the generated parser walks the
element's own prefixes (`WITH TIME ZONE`), consumes only on a proven match,
and returns diverging input (`WITH UNIQUE KEYS`) to the caller. Open element
languages (Pratt continuations, depth-cut sequences) keep the greedy legacy
selector because their bounded sequences approximate the real language.

Consequences:

- `cargo test --package pg-sql --lib` reports 1082 passed; 0 failed;
  2 ignored. Both handoff regressions pass.
- The last diagnostic-outcome accepted legacy gap (join.sql:171,
  `DELETE FROM t3 USING t1 JOIN t2 USING (a) ...`) resolved to a pass and is
  reconciled through the accepted-gap ledger with oracle parse-tree equality
  verified on macOS.
- `JsonUniqueKeys` stores the `KEYS` noise word with `#[presence(KEYS)]` so
  it round-trips; erased `optional(...)` groups cannot.
- Workspace Clippy and `cargo fmt` are clean. The 225 RCA0300/0301 analysis
  warnings remain (see below).

Recursa `main` additionally carries the semantic generation identity (neutral
edits keep the published generation, preserving incremental compilation) and
the pg-proto railroad-diagram port (shortened labels, first-reference
inlining, corrected SVG sizing).

## Save points

- Recursa `main`: `780ec62` — capability, review hardening, diagram port.
- pg-sql `main`: the merge of `port/resume-2026-09-01`; `.recursa-revision`
  pins `780ec62`. Nothing is pushed; no GitHub issues were closed. Remote
  publication still needs authorization.

## Known-red and deferred

- `tests/fmt.rs` (SQL formatter goldens) fails identically before and after
  this work: mechanical spacing (`':: TIMESTAMP'`, `AS(`) and keyword-case
  canonicalization. This is #16 provenance-aware formatting scope. The
  cast-boundary embedded test asserts the boundary structurally and pins the
  current canonical rendering; restore source-exact output under #16.
- The differential baseline still pins join.sql at pass=716/skip=6. The
  Linux sweep tolerates skip-expected/pass-actual, so this is coherence debt
  for the #19 parity run, not a failure.
- Handoff step 4 (classify the 225 distinct RCA0300/0301 warnings) is not
  started. Do not deduplicate presentation; determine real ambiguity versus
  conservative false positives — many RCA0300 sites now carry a proving
  decision, so the warning policy deserves revisiting alongside it.
- Recursa follow-ups: a runtime end-to-end fixture for suffix-proving
  viability (unit coverage exists at the decision-plan seam; pg-sql is the
  de-facto e2e); a cutoff-reachability pre-gate so open elements skip
  FIRST-5 materialization (~4.6s build-script cost today); ParseError debug
  verbosity (unchanged from the prior handoff).
- `scripts/tests/verify-workspace.sh` needs a real `ripgrep` binary; the
  interactive `rg` shell function is not visible to scripts.

## Next

Continue at #10 (strict SQL documents), which is blocked by recursa#98
(construction-free document framing). The remaining sequence and the Recursa
capability issues (#90, #93, #94, #96–#99) are unchanged from
`handoff-2026-08-31.md`.
